# Code generated by candi v1.17.15.

schema {
	query: Query
	mutation: Mutation
	subscription: Subscription
}

enum AuthTypeDirective {
	BASIC
	BEARER
	MULTIPLE
}
directive @auth(authType: AuthTypeDirective!) on FIELD_DEFINITION
directive @permissionACL(permissionCode: String!) on FIELD_DEFINITION

type Query {
	book: BookQueryResolver @auth(authType: BEARER)
	# @candi:queryRoot
}

type Mutation {
	book: BookMutationResolver @auth(authType: BEARER)
	# @candi:mutationRoot
}

type Subscription {
	book: BookSubscriptionResolver
	# @candi:subscriptionRoot
}
